
const ColoredLine = ({ color }) => (
    <hr
        style={{
            color: "",
            backgroundColor: color,
            height: 2,
            opacity: 0.2
        }}
    />
)

export default ColoredLine